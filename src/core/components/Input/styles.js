import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    input: {
        borderColor: '#9DE6E5',
        borderWidth: 3,
        width: '100%',
        borderRadius: 7
    }
  });

export default styles;